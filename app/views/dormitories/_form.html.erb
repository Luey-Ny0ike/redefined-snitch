<%= form_with(model: dormitory, local: true) do |form| %>
  <% if dormitory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dormitory.errors.count, "error") %> prohibited this dormitory from being saved:</h2>

      <ul>
      <% dormitory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :dormitory_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
